name: Pull Request

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  code_quality_checks:
    uses: ./.github/workflows/_code-quality-checks.yml
  run:
    uses: ./.github/workflows/_run.yml
    needs: code_quality_checks
